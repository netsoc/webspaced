version: '3'
services:
  app:
    image: netsoc/webspace-ng/webspaced
    build:
      context: .
      dockerfile: Dockerfile.release
    environment:
      - LXD_SOCKET=/run/lxd.sock
    volumes:
      - ./sockets:/run/webspaced
      - /var/lib/lxd/unix.socket:/run/lxd.sock
